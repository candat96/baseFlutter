import 'package:flutter/cupertino.dart';

class ErrorsPage extends StatelessWidget {
  const ErrorsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("errors page"),
    );
  }
}
